{
  "name": "lens-browser-bridge",
  "version": "1.0.1",
  "private": true,
  "description": "Minimal Playwright browser automation bridge for Lens (Kujo ecosystem). Captures screenshots, console logs, network events, and DOM summaries.",
  "main": "browser-bridge.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "install-browser": "playwright-core install chromium"
  },
  "dependencies": {
    "axe-core": "^4.11.4",
    "playwright-core": "^1.52.0"
  }
}
